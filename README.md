# NM Finance

Monthly balance accountability app for Next Millionaire MBS. Members log rent income,
contributions, and car-related expenses; the dashboard shows company balance, profit,
and per-member investment/dividend history.

## Stack
- **Frontend:** React + Vite + Tailwind
- **Backend:** Netlify Functions (serverless)
- **Database:** Netlify DB (built-in Postgres)
- **Auth:** Netlify Identity
- **Hosting:** Netlify (free tier)

No external services beyond GitHub + Netlify.

## Status
All four screens are built: Dashboard, Ledger (full CRUD + filters), Members, and
Cars. Backed by Netlify Functions (`/api/dashboard`, `/api/members`, `/api/cars`,
`/api/transactions`, `/api/init`) against Netlify DB.

## Dashboard

The dashboard (`GET /api/dashboard?month=YYYY-MM`, implemented in
`netlify/functions/dashboard.js`) aggregates company balance, monthly
profit, a 6-month cash-flow trend, per-member investment vs dividends, and
recent activity straight from the database.

Run the schema first (`db/schema.sql`) so the tables exist. If the database
isn't reachable, the frontend falls back to clearly-labelled sample data so
the UI remains viewable.

Query Netlify DB locally with:

```
netlify database status --show-credentials
```

> **Note:** the dashboard API is currently unauthenticated — it trusts the
> frontend (which sits behind Identity) and returns aggregate figures. If the
> site is ever public, add JWT verification (`Authorization: Bearer …` against
> the Identity JWT secret) to `netlify/functions/dashboard.js`.

## Auth (Netlify Identity)

The app uses the headless `gotrue-js` client against the same-origin
`/.netlify/identity` endpoint, with a custom login screen (`src/pages/LoginPage.jsx`)
covering sign in, sign up, email confirmation, password recovery, and OAuth
providers.

**To switch on auth:**

1. In the Netlify dashboard → **Site settings → Identity**, click **Enable Identity**.
2. Set **Registration preferences** (allow open sign-ups, or use invites).
3. Under **External providers**, enable GitHub/Google/etc. if you want OAuth buttons.
4. (Optional) Under **Emails & templates**, edit the confirmation/recovery email
   subjects — they're generated by Netlify.

**Local development:** run `netlify dev` (not plain `vite`) so the Identity
endpoint is served at `http://localhost:8888/.netlify/identity`. Create a local
user with:

```
netlify identity:create --confirm
```

`--confirm` skips email confirmation locally.

## Local setup (once you have Node + Netlify CLI)
```
npm install
netlify dev
```
This runs the frontend and functions together at http://localhost:8888.

## Database
Schema lives in `db/schema.sql`. Run it once against the Netlify DB instance
(via `netlify db` CLI or the Netlify dashboard SQL console) after the site is
first deployed and the database is provisioned.
